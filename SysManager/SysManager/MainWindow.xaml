<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SysManager · License: MIT -->
<Window x:Class="SysManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:vm="clr-namespace:SysManager.ViewModels"
        Title="{Binding Title}"
        Width="1280" Height="820"
        MinWidth="960" MinHeight="640"
        ui:WindowHelper.UseModernWindowStyle="True"
        Background="{StaticResource Surface0}"
        Icon="Resources/app.ico"
        AutomationProperties.AutomationId="MainWindow">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ================= Left nav ================= -->
        <Border Grid.Column="0"
                Background="{StaticResource Surface1}"
                BorderBrush="{StaticResource Border1}"
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Margin="20,20,20,16">
                    <TextBlock Text="SysManager" FontSize="20" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimary}"
                               FontFamily="{StaticResource UiFont}"/>
                    <TextBlock Text="System toolkit" Style="{StaticResource Caption}" Margin="0,2,0,0"/>
                </StackPanel>

                <!-- Nav tree -->
                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding NavGroups}"
                              Padding="10,0"
                              Focusable="False">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2">
                                <!-- Single-item group: render as a flat clickable row -->
                                <Border x:Name="SingleBd"
                                        Visibility="{Binding IsSingleItem, Converter={StaticResource FlexVis}}"
                                        CornerRadius="8" Padding="14,10" Cursor="Hand"
                                        Background="Transparent"
                                        MouseLeftButtonUp="SingleGroup_Click"
                                        Tag="{Binding Children[0]}"
                                        AutomationProperties.AutomationId="{Binding Children[0].Id}">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource Surface2}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Glyph}"
                                                   FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets"
                                                   FontSize="14" Margin="0,0,12,0"
                                                   VerticalAlignment="Center"
                                                   Foreground="{StaticResource TextSecondary}"/>
                                        <TextBlock Text="{Binding Label}"
                                                   VerticalAlignment="Center"
                                                   FontFamily="{StaticResource UiFont}"
                                                   FontSize="13" FontWeight="Medium"
                                                   Foreground="{StaticResource TextSecondary}"/>
                                    </StackPanel>
                                </Border>

                                <!-- Multi-item group: expander with children -->
                                <Expander Visibility="{Binding IsSingleItem, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"
                                          IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                                          Padding="0" Margin="0"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Foreground="{StaticResource TextSecondary}">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal" Margin="4,4,0,4">
                                            <TextBlock Text="{Binding Glyph}"
                                                       FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets"
                                                       FontSize="13" Margin="0,0,10,0"
                                                       VerticalAlignment="Center"
                                                       Foreground="{StaticResource TextMuted}"/>
                                            <TextBlock Text="{Binding Label}"
                                                       VerticalAlignment="Center"
                                                       FontFamily="{StaticResource UiFont}"
                                                       FontSize="11" FontWeight="SemiBold"
                                                       Foreground="{StaticResource TextMuted}"/>
                                        </StackPanel>
                                    </Expander.Header>
                                    <ItemsControl ItemsSource="{Binding Children}" Focusable="False">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border x:Name="ChildBd"
                                                        CornerRadius="8" Padding="28,9,14,9"
                                                        Cursor="Hand" Background="Transparent"
                                                        MouseLeftButtonUp="NavChild_Click"
                                                        Tag="{Binding}"
                                                        AutomationProperties.AutomationId="{Binding Id}">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="{StaticResource Surface2}"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Grid>
                                                        <Rectangle x:Name="ActiveMark"
                                                                   HorizontalAlignment="Left"
                                                                   Width="3" Height="18"
                                                                   Fill="{StaticResource Accent}"
                                                                   RadiusX="2" RadiusY="2"
                                                                   Margin="-28,0,0,0"
                                                                   Visibility="Collapsed"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="{Binding Glyph}"
                                                                       FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets"
                                                                       FontSize="13" Margin="0,0,10,0"
                                                                       VerticalAlignment="Center"
                                                                       Foreground="{StaticResource TextSecondary}"/>
                                                            <TextBlock Text="{Binding Label}"
                                                                       VerticalAlignment="Center"
                                                                       FontFamily="{StaticResource UiFont}"
                                                                       FontSize="13" FontWeight="Medium"
                                                                       Foreground="{StaticResource TextSecondary}"/>
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Expander>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                </ScrollViewer>

                <!-- Footer -->
                <StackPanel Grid.Row="2" Margin="20,10,20,16">
                    <Border Background="{StaticResource Surface3}" CornerRadius="999"
                            Padding="10,3" HorizontalAlignment="Left"
                            AutomationProperties.AutomationId="ElevationBadge">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE72E;"
                                       FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets"
                                       FontSize="11" Margin="0,0,6,0" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsElevated}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource Accent}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Text="{Binding ElevationBadge}"
                                       FontSize="11" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextSecondary}"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Background tasks mini-tray: deep cleanup / disk analyzer / duplicates -->
                    <Border Margin="0,10,0,0" Padding="10,8" CornerRadius="8"
                            Background="#0F1A17" BorderBrush="#22C55E" BorderThickness="1"
                            Cursor="Hand">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DeepCleanup.IsScanning}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding DeepCleanup.IsCleaning}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding DeepCleanup.IsLargeScanning}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding DiskAnalyzer.IsBusy}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding DuplicateFile.IsBusy}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Cleanup.IsSfcRunning}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Cleanup.IsDismRunning}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SystemHealth.IsChkdskRunning}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding OpenDeepCleanupTabCommand}"/>
                        </Border.InputBindings>
                        <StackPanel>
                            <!-- Deep cleanup scan line -->
                            <StackPanel Visibility="{Binding DeepCleanup.IsScanning, Converter={StaticResource FlexVis}}">
                                <TextBlock Text="Deep cleanup · scan" FontSize="11" FontWeight="SemiBold" Foreground="#22C55E"/>
                                <TextBlock Text="{Binding DeepCleanup.ScanStatusLine}" FontSize="10" Foreground="{StaticResource TextSecondary}" TextTrimming="CharacterEllipsis" Margin="0,2,0,4"/>
                                <ProgressBar Height="3" Minimum="0" Maximum="100" Value="{Binding DeepCleanup.ScanProgress}"/>
                            </StackPanel>
                            <!-- Deep cleanup clean line -->
                            <StackPanel Visibility="{Binding DeepCleanup.IsCleaning, Converter={StaticResource FlexVis}}" Margin="0,4,0,0">
                                <TextBlock Text="Deep cleanup · clean" FontSize="11" FontWeight="SemiBold" Foreground="#22C55E"/>
                                <TextBlock Text="{Binding DeepCleanup.CleanStatusLine}" FontSize="10" Foreground="{StaticResource TextSecondary}" TextTrimming="CharacterEllipsis" Margin="0,2,0,4"/>
                                <ProgressBar Height="3" Minimum="0" Maximum="100" Value="{Binding DeepCleanup.CleanProgress}"/>
                            </StackPanel>
                            <!-- Large files scan line -->
                            <StackPanel Visibility="{Binding DeepCleanup.IsLargeScanning, Converter={StaticResource FlexVis}}" Margin="0,4,0,0">
                                <TextBlock Text="Large files · scan" FontSize="11" FontWeight="SemiBold" Foreground="#22C55E"/>
                                <TextBlock FontSize="10" Foreground="{StaticResource TextSecondary}" TextTrimming="CharacterEllipsis" Margin="0,2,0,4">
                                    <Run Text="{Binding DeepCleanup.LargeFilesScanned, Mode=OneWay, StringFormat={}{0:N0} files}"/>
                                    <Run Text=" · "/>
                                    <Run Text="{Binding DeepCleanup.LargeBytesScannedDisplay, Mode=OneWay}"/>
                                </TextBlock>
                                <ProgressBar Height="3" IsIndeterminate="True"/>
                            </StackPanel>
                            <!-- Disk Analyzer progress -->
                            <StackPanel Visibility="{Binding DiskAnalyzer.IsBusy, Converter={StaticResource FlexVis}}" Margin="0,4,0,0">
                                <TextBlock Text="Disk Analyzer · scan" FontSize="11" FontWeight="SemiBold" Foreground="#22C55E"/>
                                <TextBlock Text="{Binding DiskAnalyzer.CurrentFolder}" FontSize="10" Foreground="{StaticResource TextSecondary}" TextTrimming="CharacterEllipsis" Margin="0,2,0,4"/>
                                <ProgressBar Height="3" IsIndeterminate="True"/>
                            </StackPanel>
                            <!-- Duplicate File progress -->
                            <StackPanel Visibility="{Binding DuplicateFile.IsBusy, Converter={StaticResource FlexVis}}" Margin="0,4,0,0">
                                <TextBlock Text="Duplicates · scan" FontSize="11" FontWeight="SemiBold" Foreground="#22C55E"/>
                                <TextBlock Text="{Binding DuplicateFile.StatusMessage}" FontSize="10" Foreground="{StaticResource TextSecondary}" TextTrimming="CharacterEllipsis" Margin="0,2,0,4"/>
                                <ProgressBar Height="3" IsIndeterminate="True"/>
                            </StackPanel>
                            <!-- SFC /scannow progress -->
                            <StackPanel Visibility="{Binding Cleanup.IsSfcRunning, Converter={StaticResource FlexVis}}" Margin="0,4,0,0">
                                <TextBlock Text="SFC /scannow" FontSize="11" FontWeight="SemiBold" Foreground="#22C55E"/>
                                <TextBlock Text="{Binding Cleanup.SfcStatus}" FontSize="10" Foreground="{StaticResource TextSecondary}" TextTrimming="CharacterEllipsis" Margin="0,2,0,4"/>
                                <ProgressBar Height="3" IsIndeterminate="True"/>
                            </StackPanel>
                            <!-- DISM RestoreHealth progress -->
                            <StackPanel Visibility="{Binding Cleanup.IsDismRunning, Converter={StaticResource FlexVis}}" Margin="0,4,0,0">
                                <TextBlock Text="DISM RestoreHealth" FontSize="11" FontWeight="SemiBold" Foreground="#60A5FA"/>
                                <TextBlock Text="{Binding Cleanup.DismStatus}" FontSize="10" Foreground="{StaticResource TextSecondary}" TextTrimming="CharacterEllipsis" Margin="0,2,0,4"/>
                                <ProgressBar Height="3" IsIndeterminate="True"/>
                            </StackPanel>
                            <!-- chkdsk progress -->
                            <StackPanel Visibility="{Binding SystemHealth.IsChkdskRunning, Converter={StaticResource FlexVis}}" Margin="0,4,0,0">
                                <TextBlock Text="chkdsk · file system scan" FontSize="11" FontWeight="SemiBold" Foreground="#FBBF24"/>
                                <TextBlock Text="{Binding SystemHealth.ChkdskStatus}" FontSize="10" Foreground="{StaticResource TextSecondary}" TextTrimming="CharacterEllipsis" Margin="0,2,0,4"/>
                                <ProgressBar Height="3" IsIndeterminate="True"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="{Binding About.CurrentVersion, StringFormat=v{0}}" Style="{StaticResource Caption}" Margin="0,8,0,0"/>
                    <TextBlock Text="by laurentiu021" Style="{StaticResource Caption}" Margin="0,2,0,0" Opacity="0.7"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ================= Content ================= -->
        <Border Grid.Column="1"
                Background="{StaticResource Surface0}"
                AutomationProperties.AutomationId="ContentHost"
                AutomationProperties.Name="ContentHost">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Update banner — only when a newer release is available. -->
                <Border Grid.Row="0" Margin="24,16,24,0" Padding="14,10" CornerRadius="10"
                        Background="#0F1A2E" BorderBrush="{StaticResource Accent}" BorderThickness="1"
                        Visibility="{Binding About.UpdateAvailable, Converter={StaticResource FlexVis}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock Text="&#xE896;" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets"
                                       FontSize="16" Foreground="{StaticResource Accent}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="Update available" FontWeight="SemiBold" Foreground="{StaticResource Accent}" VerticalAlignment="Center"/>
                            <TextBlock Text="  ·  " Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding About.LatestVersionLabel}" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                            <TextBlock Text="  ·  " Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding About.LatestPublishedLabel}" Foreground="{StaticResource TextSecondary}" FontSize="12" VerticalAlignment="Center"/>
                        </StackPanel>
                        <Button Grid.Column="1" Content="View details" Command="{Binding OpenAboutTabCommand}"
                                Style="{StaticResource PrimaryButton}" Padding="14,6"/>
                    </Grid>
                </Border>

                <!-- Background task banner — deep cleanup scan / large files scan -->
                <!-- (moved to sidebar footer — see left nav bottom section) -->

                <!-- (update banner continues below) -->

                <ContentControl Grid.Row="1" x:Name="ContentHost"
                                Content="{Binding SelectedNav.View}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
