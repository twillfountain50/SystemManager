<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SysManager · License: MIT -->
<UserControl x:Class="SysManager.Views.CleanupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:v="clr-namespace:SysManager.Views">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="PageBg" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#070A0F" Offset="0"/>
            <GradientStop Color="#0B1220" Offset="0.5"/>
            <GradientStop Color="#090D16" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Resources>

    <Grid Background="{StaticResource PageBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="28,24,28,0">
            <TextBlock Text="Cleanup" Style="{StaticResource Display}"/>
            <TextBlock Text="Clear temp files, empty Recycle Bin, or run SFC and DISM to repair Windows."
                       Style="{StaticResource Subtle}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Admin banner — SFC/DISM need it -->
        <Border Grid.Row="1" Margin="28,16,28,0" Padding="16,14" CornerRadius="10"
                Background="#2A1E14" BorderBrush="#F59E0B" BorderThickness="1"
                Visibility="{Binding IsElevated, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE7EF;" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets"
                                   FontSize="18" Foreground="#F59E0B" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="SFC and DISM need administrator rights"
                                   FontSize="15" FontWeight="SemiBold" Foreground="#F59E0B" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="Temp cleanup and Recycle Bin work without admin. SFC /scannow and DISM /RestoreHealth can only be launched elevated."
                               Foreground="{StaticResource TextSecondary}" Margin="28,6,0,0" TextWrapping="Wrap"/>
                </StackPanel>
                <Button Grid.Column="1" Content="Run as Administrator"
                        Command="{Binding RelaunchAsAdminCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Padding="18,10" FontSize="14" VerticalAlignment="Center" Margin="16,0,0,0"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="28,16,28,0">
            <StackPanel>
                <!-- Pre-scan summary so the tab isn't empty on first load -->
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Style="{StaticResource CardInner}" Margin="0,0,6,0">
                        <StackPanel>
                            <TextBlock Text="TEMP FOLDERS" Style="{StaticResource Caption}"/>
                            <TextBlock Text="{Binding TempSizeLabel}" FontSize="16" FontWeight="SemiBold"
                                       Foreground="#60A5FA" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="1" Style="{StaticResource CardInner}" Margin="6,0,0,0">
                        <StackPanel>
                            <TextBlock Text="RECYCLE BIN" Style="{StaticResource Caption}"/>
                            <TextBlock Text="{Binding RecycleBinLabel}" FontSize="16" FontWeight="SemiBold"
                                       Foreground="#FBBF24" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <TextBlock Text="Actions" Style="{StaticResource SectionTitle}" Margin="0,0,0,4"/>
                <TextBlock Text="Clean TEMP removes temporary files from %TEMP% and Windows\Temp. SFC /scannow checks Windows system files for corruption and repairs them. DISM /RestoreHealth repairs the Windows component store using Windows Update as a source."
                           Style="{StaticResource Subtle}" Margin="0,0,0,10" TextWrapping="Wrap"/>
                <WrapPanel Orientation="Horizontal">
                    <Button Content="Clean TEMP" Command="{Binding CleanTempCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,8"/>
                    <Button Content="Empty Recycle Bin" Command="{Binding EmptyRecycleBinCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8"/>
                    <Button Content="⟳ Rescan" Command="{Binding RescanCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8"/>
                    <Button Content="SFC /scannow" Command="{Binding RunSfcCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8"
                            IsEnabled="{Binding IsSfcRunning, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>
                    <Button Content="DISM RestoreHealth" Command="{Binding RunDismCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8"
                            IsEnabled="{Binding IsDismRunning, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>
                    <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource GhostButton}" Margin="0,0,8,8"/>
                </WrapPanel>

                <!-- Background-task indicators. Stay visible while SFC/DISM
                     are running so the user can navigate away freely. -->
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0"
                            Visibility="{Binding IsSfcRunning, Converter={StaticResource FlexVis}}">
                    <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,8,0" Fill="#22C55E"/>
                    <TextBlock Text="SFC /scannow is running in background — " Foreground="#22C55E" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding SfcStatus}" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                </StackPanel>
                <!-- SFC verdict (shown after completion) -->
                <Border Margin="0,8,0,0" Padding="12,10" CornerRadius="8"
                        Visibility="{Binding SfcVerdict, Converter={StaticResource FlexVis}}">
                    <Border.Background>
                        <SolidColorBrush Color="#0D1117" Opacity="0.8"/>
                    </Border.Background>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="SFC result:" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}"
                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding SfcVerdict}" TextWrapping="Wrap" VerticalAlignment="Center"
                                   Foreground="{Binding SfcVerdictColorHex, Converter={StaticResource HexBrush}}"/>
                    </StackPanel>
                </Border>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0"
                            Visibility="{Binding IsDismRunning, Converter={StaticResource FlexVis}}">
                    <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,8,0" Fill="#60A5FA"/>
                    <TextBlock Text="DISM is running in background — " Foreground="#60A5FA" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding DismStatus}" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                </StackPanel>
                <!-- DISM verdict (shown after completion) -->
                <Border Margin="0,8,0,0" Padding="12,10" CornerRadius="8"
                        Visibility="{Binding DismVerdict, Converter={StaticResource FlexVis}}">
                    <Border.Background>
                        <SolidColorBrush Color="#0D1117" Opacity="0.8"/>
                    </Border.Background>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="DISM result:" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}"
                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding DismVerdict}" TextWrapping="Wrap" VerticalAlignment="Center"
                                   Foreground="{Binding DismVerdictColorHex, Converter={StaticResource HexBrush}}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource Card}" Margin="28,16,28,0" Padding="0">
            <v:ConsoleView DataContext="{Binding Console}"/>
        </Border>

        <Grid Grid.Row="4" Margin="28,12,28,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ProgressBar Grid.Column="0" Height="6" Minimum="0" Maximum="100"
                         Value="{Binding Progress}"
                         IsIndeterminate="{Binding IsProgressIndeterminate}"/>
            <TextBlock Grid.Column="1" Margin="12,0,0,0" Text="{Binding StatusMessage}" Style="{StaticResource Subtle}"/>
        </Grid>
    </Grid>
</UserControl>
