<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SysManager · License: MIT -->
<UserControl x:Class="SysManager.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="System overview" FontSize="22" FontWeight="SemiBold"/>
            <Border CornerRadius="10" Padding="10,2" Margin="16,4,0,0"
                    Background="#1F6FEB" Visibility="{Binding IsElevated, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="Administrator" Foreground="White" FontSize="12"/>
            </Border>
            <Button Margin="16,0,0,0" Content="Request admin"
                    Command="{Binding RequestElevationCommand}">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsElevated}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <Button Grid.Row="1" Margin="0,12,0,12" HorizontalAlignment="Left"
                Content="Scan system" Command="{Binding RefreshCommand}"
                Padding="14,6" FontWeight="SemiBold"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border CornerRadius="8" Padding="14" Background="#161B22" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="Operating System" Foreground="#9AA0A6" FontSize="12"/>
                        <TextBlock Text="{Binding OsLine}" FontSize="15" Margin="0,4,0,0"/>
                        <TextBlock Text="{Binding UptimeLine}" Foreground="#9AA0A6" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>

                <Border CornerRadius="8" Padding="14" Background="#161B22" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="CPU" Foreground="#9AA0A6" FontSize="12"/>
                        <TextBlock Text="{Binding CpuLine}" FontSize="15" Margin="0,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <Border CornerRadius="8" Padding="14" Background="#161B22" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="Memory" Foreground="#9AA0A6" FontSize="12"/>
                        <TextBlock Text="{Binding MemLine}" FontSize="15" Margin="0,4,0,6"/>
                        <ProgressBar Height="8" Minimum="0" Maximum="100"
                                     Value="{Binding Snapshot.Memory.UsedPercent}"/>
                    </StackPanel>
                </Border>

                <Border CornerRadius="8" Padding="14" Background="#161B22" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="Storage" Foreground="#9AA0A6" FontSize="12"/>
                        <TextBlock Text="{Binding DiskLine}" FontSize="14" Margin="0,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ProgressBar Grid.Column="0" Height="4"
                         IsIndeterminate="{Binding IsProgressIndeterminate}"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
            <TextBlock Grid.Column="1" Margin="12,0,0,0" Text="{Binding StatusMessage}" Foreground="#9AA0A6"/>
        </Grid>
    </Grid>
</UserControl>
