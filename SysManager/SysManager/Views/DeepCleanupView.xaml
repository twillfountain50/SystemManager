<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SysManager · License: MIT -->
<UserControl x:Class="SysManager.Views.DeepCleanupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="PageBg" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#070A0F" Offset="0"/>
            <GradientStop Color="#0B1220" Offset="0.5"/>
            <GradientStop Color="#090D16" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Resources>

    <Grid Background="{StaticResource PageBg}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="28,24,28,28">

                <!-- Header -->
                <DockPanel>
                    <StackPanel>
                        <TextBlock Text="Deep cleanup" Style="{StaticResource Display}"/>
                        <TextBlock Text="Find safe-to-remove installer leftovers, caches and large files. Everything is scan-first — you pick what goes."
                                   Style="{StaticResource Subtle}" Margin="0,4,0,0" TextWrapping="Wrap" MaxWidth="820"/>
                    </StackPanel>
                </DockPanel>

                <!-- Safety note -->
                <Border Style="{StaticResource Card}" Margin="0,16,0,0"
                        Background="#122030" BorderBrush="{StaticResource Accent}" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE946;" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets"
                                   FontSize="18" Foreground="{StaticResource Accent}" VerticalAlignment="Top" Margin="0,2,12,0"/>
                        <StackPanel>
                            <TextBlock Text="Safe by design" FontWeight="SemiBold" Foreground="{StaticResource Accent}"/>
                            <TextBlock TextWrapping="Wrap" Foreground="{StaticResource TextSecondary}" MaxWidth="900" Margin="0,4,0,0" FontSize="12">
                                Only locations documented as safe by vendors (NVIDIA, AMD, Microsoft) are scanned. We never touch browser caches, passwords,
                                live app settings, the registry, active drivers, or Program Files. A file that's locked or in use is skipped — nothing is forced.
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- SCAN + CLEAN -->
                <Border Style="{StaticResource Card}" Margin="0,16,0,0">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,8">
                            <TextBlock Text="Safe-to-clean categories" Style="{StaticResource SectionTitle}"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                                <Button Content="Scan" Command="{Binding ScanCommand}" Style="{StaticResource PrimaryButton}"
                                        IsEnabled="{Binding IsScanning, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>
                                <Button Content="Select all safe" Command="{Binding SelectAllCommand}" CommandParameter="{StaticResource TrueValue}"
                                        Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
                                <Button Content="Clear" Command="{Binding SelectAllCommand}" CommandParameter="{StaticResource FalseValue}"
                                        Style="{StaticResource GhostButton}" Margin="8,0,0,0"/>
                            </StackPanel>
                        </DockPanel>
                        <TextBlock Text="{Binding ScanSummary}" Style="{StaticResource Subtle}" Margin="0,0,0,10" TextWrapping="Wrap"/>

                        <!-- Scan progress — determinate, category-by-category -->
                        <StackPanel Margin="0,0,0,10" Visibility="{Binding IsScanning, Converter={StaticResource FlexVis}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding ScanStatusLine}" Foreground="{StaticResource Accent}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding ScanProgress, StringFormat={}{0}%}" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="12,0,0,0"/>
                            </Grid>
                            <ProgressBar Height="6" Minimum="0" Maximum="100" Value="{Binding ScanProgress}" Margin="0,6,0,0"/>
                        </StackPanel>

                        <ItemsControl ItemsSource="{Binding Categories}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource CardInner}" Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                            <StackPanel Grid.Column="1">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" Foreground="{StaticResource TextPrimary}"/>
                                                    <Border Margin="10,0,0,0" Padding="8,2" CornerRadius="999"
                                                            Background="#5C1B1B" BorderBrush="#EF4444" BorderThickness="1"
                                                            Visibility="{Binding IsDestructiveHint, Converter={StaticResource BoolToVis}}">
                                                        <TextBlock Text="Irreversible" Foreground="#FCA5A5" FontSize="11" FontWeight="SemiBold"/>
                                                    </Border>
                                                </StackPanel>
                                                <TextBlock Text="{Binding Description}" Style="{StaticResource Subtle}" Margin="0,4,0,0" TextWrapping="Wrap" MaxWidth="820"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10,0,0,0">
                                                <TextBlock Text="{Binding SizeDisplay}" FontWeight="SemiBold" FontSize="14" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Right"/>
                                                <TextBlock Text="{Binding CountDisplay}" Style="{StaticResource Caption}" HorizontalAlignment="Right"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Clean action row -->
                        <Border Style="{StaticResource CardInner}" Margin="0,8,0,0" Background="#0E141D">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="Selected total:" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding TotalSelectedDisplay}" FontWeight="SemiBold" FontSize="16"
                                               Foreground="{StaticResource Accent}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="Clean selected" Command="{Binding CleanCommand}"
                                            Style="{StaticResource PrimaryButton}"
                                            IsEnabled="{Binding IsCleaning, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>
                                    <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource GhostButton}" Margin="8,0,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <TextBlock Text="{Binding CleanSummary}" Style="{StaticResource Subtle}" Margin="0,8,0,0" TextWrapping="Wrap"
                                   Visibility="{Binding CleanSummary, Converter={StaticResource FlexVis}}"/>

                        <!-- Clean progress — determinate, category-by-category -->
                        <StackPanel Margin="0,8,0,0" Visibility="{Binding IsCleaning, Converter={StaticResource FlexVis}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding CleanStatusLine}" Foreground="#22C55E" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding CleanProgress, StringFormat={}{0}%}" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="12,0,0,0"/>
                            </Grid>
                            <ProgressBar Height="6" Minimum="0" Maximum="100" Value="{Binding CleanProgress}" Margin="0,6,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- LARGE FILES -->
                <Border Style="{StaticResource Card}" Margin="0,16,0,0">
                    <StackPanel>
                        <DockPanel>
                            <TextBlock Text="Large files finder" Style="{StaticResource SectionTitle}"/>
                            <Button Content="Scan drive" Command="{Binding ScanLargeFilesCommand}"
                                    Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" DockPanel.Dock="Right"
                                    IsEnabled="{Binding IsLargeScanning, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>
                        </DockPanel>
                        <TextBlock Text="Read-only — shows biggest files so you know where your space went. Results have 'Show in Explorer' and 'Copy path' only. Deletion is disabled by design, even with admin."
                                   Style="{StaticResource Subtle}" Margin="0,4,0,10" TextWrapping="Wrap"/>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Location:" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox Grid.Column="1" ItemsSource="{Binding ScanLocations}" SelectedItem="{Binding SelectedLocation, Mode=TwoWay}"
                                      DisplayMemberPath="Label" MaxWidth="380" HorizontalAlignment="Left"/>
                            <TextBlock Grid.Column="2" Text="Min size (MB):" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="16,0,8,0"/>
                            <TextBox Grid.Column="3" Text="{Binding MinSizeMB, Mode=TwoWay}" Width="80"/>
                            <TextBlock Grid.Column="4" Text="Top:" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="16,0,8,0"/>
                            <TextBox Grid.Column="5" Text="{Binding TopCount, Mode=TwoWay}" Width="60"/>
                        </Grid>

                        <TextBlock Text="{Binding LargeScanStatus}" Style="{StaticResource Subtle}" Margin="0,0,0,8"
                                   Visibility="{Binding LargeScanStatus, Converter={StaticResource FlexVis}}"/>

                        <!-- Large-files scan progress — indeterminate bar + live counters -->
                        <StackPanel Margin="0,0,0,10" Visibility="{Binding IsLargeScanning, Converter={StaticResource FlexVis}}">
                            <ProgressBar Height="6" IsIndeterminate="True"/>
                            <Grid Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding LargeFilesScanned, StringFormat={}{0:N0} files}" Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="  ·  " Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="{Binding LargeBytesScannedDisplay, StringFormat=scanned {0}}" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                                <Button Grid.Column="3" Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource GhostButton}" Padding="10,4"/>
                            </Grid>
                            <TextBlock Text="{Binding LargeCurrentFolder}" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <DataGrid ItemsSource="{Binding LargeFiles}"
                                  AutoGenerateColumns="False" HeadersVisibility="Column"
                                  GridLinesVisibility="None" IsReadOnly="True"
                                  Background="Transparent" RowBackground="Transparent"
                                  AlternatingRowBackground="#0F141C" BorderThickness="0"
                                  MaxHeight="380">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Size" Binding="{Binding SizeDisplay}" Width="110"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Modified" Binding="{Binding LastModifiedDisplay}" Width="110"/>
                                <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="2*"/>
                                <DataGridTemplateColumn Header="Actions" Width="210">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="Show" Padding="10,4"
                                                        Command="{Binding DataContext.ShowInExplorerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding Path}"
                                                        Style="{StaticResource SecondaryButton}"/>
                                                <Button Content="Copy" Padding="10,4" Margin="6,0,0,0"
                                                        Command="{Binding DataContext.CopyPathCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding Path}"
                                                        Style="{StaticResource GhostButton}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
