<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SysManager · License: MIT -->
<UserControl x:Class="SysManager.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="PageBg" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#070A0F" Offset="0"/>
            <GradientStop Color="#0B1220" Offset="0.5"/>
            <GradientStop Color="#090D16" Offset="1"/>
        </LinearGradientBrush>

        <!-- Reusable "severity dot" — a glowing colored ring.
             Usage: <ContentPresenter Content="…" Resources="{StaticResource SevDotStyle}"/> -->
        <Style x:Key="SevDot" TargetType="Ellipse">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Height" Value="10"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="StrokeThickness" Value="2"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource PageBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="28,24,28,0">
            <StackPanel>
                <TextBlock Text="System logs" Style="{StaticResource Display}"/>
                <TextBlock Text="Windows Event Log, explained in plain English."
                           Style="{StaticResource Subtle}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right" VerticalAlignment="Center">
                <Button Content="Open Event Viewer" Command="{Binding OpenEventViewerCommand}" Style="{StaticResource GhostButton}"/>
                <Button Content="Open log folder" Command="{Binding OpenLogFolderCommand}" Style="{StaticResource GhostButton}" Margin="6,0,0,0"/>
                <Button Content="Export CSV" Command="{Binding ExportCsvCommand}" Style="{StaticResource SecondaryButton}" Margin="6,0,0,0"/>
            </StackPanel>
        </DockPanel>

        <!-- Severity count pills -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="28,16,28,0">
            <Border Background="#2A1114" BorderBrush="#FF3B30" BorderThickness="1" CornerRadius="999" Padding="14,8" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Style="{StaticResource SevDot}" Fill="#FF3B30" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding CriticalCount}" FontSize="14" FontWeight="SemiBold" Foreground="#FF3B30" VerticalAlignment="Center"/>
                    <TextBlock Text=" Critical" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Background="#2A1E1E" BorderBrush="#F87171" BorderThickness="1" CornerRadius="999" Padding="14,8" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Style="{StaticResource SevDot}" Fill="#F87171" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding ErrorCount}" FontSize="14" FontWeight="SemiBold" Foreground="#F87171" VerticalAlignment="Center"/>
                    <TextBlock Text=" Errors" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Background="#2A2314" BorderBrush="#FBBF24" BorderThickness="1" CornerRadius="999" Padding="14,8" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Style="{StaticResource SevDot}" Fill="#FBBF24" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding WarningCount}" FontSize="14" FontWeight="SemiBold" Foreground="#FBBF24" VerticalAlignment="Center"/>
                    <TextBlock Text=" Warnings" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Background="#11212A" BorderBrush="#38BDF8" BorderThickness="1" CornerRadius="999" Padding="14,8">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Style="{StaticResource SevDot}" Fill="#38BDF8" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding InfoCount}" FontSize="14" FontWeight="SemiBold" Foreground="#38BDF8" VerticalAlignment="Center"/>
                    <TextBlock Text=" Info" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Filter bar -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="28,16,28,0">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Log" Style="{StaticResource Subtle}" VerticalAlignment="Center"/>
                    <ComboBox ItemsSource="{Binding AvailableLogs}" SelectedItem="{Binding SelectedLog}" Width="130" Margin="8,0,18,0"/>

                    <TextBlock Text="Time" Style="{StaticResource Subtle}" VerticalAlignment="Center"/>
                    <ComboBox ItemsSource="{Binding TimeRanges}" SelectedItem="{Binding SelectedTimeRange}" Width="140" Margin="8,0,18,0"/>

                    <TextBlock Text="Max results" Style="{StaticResource Subtle}" VerticalAlignment="Center"/>
                    <ComboBox ItemsSource="{Binding MaxResultOptions}" SelectedItem="{Binding SelectedMaxResults}" Width="88" Margin="8,0,18,0"/>

                    <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource PrimaryButton}"/>
                    <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource GhostButton}" Margin="6,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                    <TextBlock Text="Show" Style="{StaticResource Subtle}" VerticalAlignment="Center" Margin="0,0,10,0"/>

                    <!-- Each checkbox has its own colored dot next to the label -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,14,0">
                        <CheckBox IsChecked="{Binding ShowCritical}" VerticalAlignment="Center"/>
                        <Ellipse Style="{StaticResource SevDot}" Fill="#FF3B30" Margin="8,0,6,0"/>
                        <TextBlock Text="Critical" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,14,0">
                        <CheckBox IsChecked="{Binding ShowError}" VerticalAlignment="Center"/>
                        <Ellipse Style="{StaticResource SevDot}" Fill="#F87171" Margin="8,0,6,0"/>
                        <TextBlock Text="Error" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,14,0">
                        <CheckBox IsChecked="{Binding ShowWarning}" VerticalAlignment="Center"/>
                        <Ellipse Style="{StaticResource SevDot}" Fill="#FBBF24" Margin="8,0,6,0"/>
                        <TextBlock Text="Warning" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,14,0">
                        <CheckBox IsChecked="{Binding ShowInfo}" VerticalAlignment="Center"/>
                        <Ellipse Style="{StaticResource SevDot}" Fill="#38BDF8" Margin="8,0,6,0"/>
                        <TextBlock Text="Info" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,22,0">
                        <CheckBox IsChecked="{Binding ShowVerbose}" VerticalAlignment="Center"/>
                        <Ellipse Style="{StaticResource SevDot}" Fill="#9AA0A6" Margin="8,0,6,0"/>
                        <TextBlock Text="Verbose" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBlock Text="Search" Style="{StaticResource Subtle}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="280"
                             ToolTip="Filters by message, provider, or event ID"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Master-detail -->
        <Grid Grid.Row="3" Margin="28,16,28,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- List -->
            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0">
                <DataGrid ItemsSource="{Binding EntriesView}"
                          SelectedItem="{Binding SelectedEntry}"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          IsReadOnly="True"
                          Background="Transparent"
                          RowBackground="Transparent"
                          AlternatingRowBackground="#0F141C"
                          BorderThickness="0"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <!-- Severity column: colored dot instead of emoji -->
                        <DataGridTemplateColumn Header="" Width="40">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Ellipse Width="10" Height="10" VerticalAlignment="Center" HorizontalAlignment="Center"
                                             Fill="{Binding SeverityColor, Converter={StaticResource HexBrush}}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="150"/>
                        <DataGridTextColumn Header="Source" Binding="{Binding ProviderName}" Width="170"/>
                        <DataGridTextColumn Header="ID" Binding="{Binding EventId}" Width="60"/>
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="{StaticResource Border1}"/>

            <!-- Detail -->
            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="0">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
                    <StackPanel>
                        <!-- Empty state -->
                        <StackPanel Visibility="{Binding SelectedEntry, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}">
                            <TextBlock Text="Select an event on the left to see details"
                                       Style="{StaticResource Subtle}" Margin="0,20,0,0"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>

                        <!-- Populated state -->
                        <StackPanel Visibility="{Binding SelectedEntry, Converter={StaticResource FlexVis}}">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,12,0"
                                         Fill="{Binding SelectedEntry.SeverityColor, Converter={StaticResource HexBrush}}"/>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding SelectedEntry.SeverityLabel}" FontSize="18" FontWeight="SemiBold"
                                                   Foreground="{Binding SelectedEntry.SeverityColor, Converter={StaticResource HexBrush}}"/>
                                        <TextBlock Text=" — Event " FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                                        <TextBlock Text="{Binding SelectedEntry.EventId}" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding SelectedEntry.ProviderName}" Style="{StaticResource Subtle}"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Meta -->
                            <Border Style="{StaticResource CardInner}" Margin="0,14,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="When" Style="{StaticResource Subtle}"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedEntry.Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss}"/>
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Log" Style="{StaticResource Subtle}"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedEntry.LogName}"/>
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Machine" Style="{StaticResource Subtle}"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedEntry.MachineName}"/>
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Record #" Style="{StaticResource Subtle}"/>
                                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedEntry.RecordId}"/>
                                </Grid>
                            </Border>

                            <TextBlock Text="What this means" Style="{StaticResource SectionTitle}" Margin="0,18,0,6"/>
                            <Border Padding="12" CornerRadius="8" Background="#102028" BorderBrush="#1E3A4A" BorderThickness="1">
                                <TextBlock Text="{Binding SelectedEntry.Explanation}" TextWrapping="Wrap" Foreground="{StaticResource TextPrimary}"/>
                            </Border>

                            <TextBlock Text="What to try" Style="{StaticResource SectionTitle}" Margin="0,14,0,6"/>
                            <Border Padding="12" CornerRadius="8" Background="#141B14" BorderBrush="#2D4A2D" BorderThickness="1">
                                <TextBlock Text="{Binding SelectedEntry.Recommendation}" TextWrapping="Wrap" Foreground="{StaticResource TextPrimary}"/>
                            </Border>

                            <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                                <Button Content="Copy details" Command="{Binding CopySelectedCommand}" Style="{StaticResource SecondaryButton}"/>
                                <Button Content="Search online" Command="{Binding SearchOnlineCommand}" Style="{StaticResource GhostButton}" Margin="6,0,0,0"/>
                            </StackPanel>

                            <Expander Header="Full system message" Margin="0,16,0,0" Foreground="{StaticResource TextSecondary}">
                                <Border Padding="10" CornerRadius="8" Background="{StaticResource Surface0}"
                                        BorderBrush="{StaticResource Border1}" BorderThickness="1">
                                    <TextBox Text="{Binding SelectedEntry.FullMessage, Mode=OneWay}"
                                             IsReadOnly="True" TextWrapping="Wrap"
                                             Background="Transparent" BorderThickness="0"
                                             FontFamily="{StaticResource MonoFont}" FontSize="11"
                                             Foreground="#C0C6CC"
                                             MaxHeight="240"
                                             VerticalScrollBarVisibility="Auto"/>
                                </Border>
                            </Expander>

                            <Expander Header="Raw XML (for power users)" Margin="0,8,0,0" Foreground="{StaticResource TextSecondary}">
                                <Border Padding="10" CornerRadius="8" Background="{StaticResource Surface0}"
                                        BorderBrush="{StaticResource Border1}" BorderThickness="1">
                                    <TextBox Text="{Binding SelectedEntry.Xml, Mode=OneWay}"
                                             IsReadOnly="True" TextWrapping="NoWrap"
                                             Background="Transparent" BorderThickness="0"
                                             FontFamily="{StaticResource MonoFont}" FontSize="11"
                                             Foreground="#8A9198"
                                             MaxHeight="260"
                                             VerticalScrollBarVisibility="Auto"
                                             HorizontalScrollBarVisibility="Auto"/>
                                </Border>
                            </Expander>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Footer -->
        <Grid Grid.Row="4" Margin="28,12,28,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" Style="{StaticResource Subtle}"/>
            <ProgressBar Grid.Column="1" Width="180" Height="4"
                         IsIndeterminate="{Binding IsProgressIndeterminate}"
                         Visibility="{Binding IsBusy, Converter={StaticResource FlexVis}}"/>
        </Grid>
    </Grid>
</UserControl>
