<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SysManager · License: MIT -->
<UserControl x:Class="SysManager.Views.NetworkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="PageBg" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#070A0F" Offset="0"/>
            <GradientStop Color="#0B1220" Offset="0.5"/>
            <GradientStop Color="#090D16" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Resources>

    <Grid Background="{StaticResource PageBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ===== Header ===== -->
        <DockPanel Grid.Row="0" Margin="28,24,28,0">
            <StackPanel>
                <TextBlock Text="Network" Style="{StaticResource Display}"/>
                <TextBlock Text="Live ping, auto-traceroute, speed tests and a verdict on where the lag is."
                           Style="{StaticResource Subtle}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right" VerticalAlignment="Center">
                <Button Content="Start" Command="{Binding StartCommand}" Style="{StaticResource PrimaryButton}"
                        Visibility="{Binding IsMonitoring, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>
                <Button Content="Stop" Command="{Binding StopCommand}" Style="{StaticResource SecondaryButton}"
                        Visibility="{Binding IsMonitoring, Converter={StaticResource FlexVis}}"/>
                <Button Content="Clear" Command="{Binding ClearHistoryCommand}" Style="{StaticResource GhostButton}" Margin="6,0,0,0"/>
            </StackPanel>
        </DockPanel>

        <!-- ===== Health verdict ===== -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="28,16,28,0"
                BorderBrush="{Binding Health.ColorHex, Converter={StaticResource HexBrush}}"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="12" Height="12" Margin="0,0,10,0" VerticalAlignment="Center"
                                 Fill="{Binding Health.ColorHex, Converter={StaticResource HexBrush}}"/>
                        <TextBlock Text="{Binding Health.Headline}" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{Binding Health.ColorHex, Converter={StaticResource HexBrush}}"/>
                    </StackPanel>
                    <TextBlock Text="{Binding Health.Detail}" Foreground="{StaticResource TextSecondary}"
                               Margin="22,6,0,0" TextWrapping="Wrap" MaxWidth="820"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Border Style="{StaticResource CardInner}" Margin="8,0" Padding="16,8" MinWidth="110">
                        <StackPanel>
                            <TextBlock Text="AVG PING" Style="{StaticResource Caption}"/>
                            <TextBlock Text="{Binding Health.AveragePingMs, StringFormat={}{0:F0} ms}"
                                       Style="{StaticResource Metric}" Foreground="#60A5FA"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource CardInner}" Margin="8,0" Padding="16,8" MinWidth="110">
                        <StackPanel>
                            <TextBlock Text="WORST LOSS" Style="{StaticResource Caption}"/>
                            <TextBlock Text="{Binding Health.WorstLossPercent, StringFormat={}{0:F1}%}"
                                       Style="{StaticResource Metric}" Foreground="#FBBF24"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource CardInner}" Margin="8,0" Padding="16,8" MinWidth="110">
                        <StackPanel>
                            <TextBlock Text="WORST JITTER" Style="{StaticResource Caption}"/>
                            <TextBlock Text="{Binding Health.WorstJitterMs, StringFormat={}{0:F0} ms}"
                                       Style="{StaticResource Metric}" Foreground="#F472B6"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ===== Targets ===== -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="28,16,28,0">
            <StackPanel>
                <DockPanel>
                    <TextBlock Text="Targets" Style="{StaticResource SectionTitle}"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right" VerticalAlignment="Center">
                        <TextBlock Text="Preset" Style="{StaticResource Subtle}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <ComboBox ItemsSource="{Binding Presets}"
                                  DisplayMemberPath="Name"
                                  SelectedItem="{Binding SelectedPreset, Mode=TwoWay}"
                                  Width="200"/>
                    </StackPanel>
                </DockPanel>
                <TextBlock Text="{Binding SelectedPreset.Description}"
                           Style="{StaticResource Subtle}" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>

                <ItemsControl ItemsSource="{Binding Targets}" Margin="0,14,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><WrapPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,10,10" Padding="12,8" CornerRadius="999"
                                    Background="{StaticResource Surface2}"
                                    BorderBrush="{StaticResource Border1}" BorderThickness="1">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,8,0"
                                             Fill="{Binding ColorHex, Converter={StaticResource HexBrush}}"/>
                                    <CheckBox IsChecked="{Binding IsEnabled}" VerticalAlignment="Center" Margin="0,0,2,0"/>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Text="  " VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Host}" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" FontSize="11"/>
                                    <Border Width="1" Height="14" Background="{StaticResource Border2}" Margin="10,0"/>
                                    <TextBlock Text="{Binding LastLatencyMs, StringFormat={}{0:F0} ms, FallbackValue=—}"
                                               VerticalAlignment="Center" Foreground="#60A5FA" FontWeight="SemiBold"/>
                                    <TextBlock Text=" · " Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                                    <TextBlock Text="avg " Style="{StaticResource Caption}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding AverageMs, StringFormat={}{0:F1}, FallbackValue=—}"
                                               VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBlock Text=" · " Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                                    <TextBlock Text="jitter " Style="{StaticResource Caption}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding JitterMs, StringFormat={}{0:F0}, FallbackValue=—}"
                                               VerticalAlignment="Center" Foreground="#F472B6"/>
                                    <TextBlock Text=" · " Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                                    <TextBlock Text="loss " Style="{StaticResource Caption}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding LossPercent, StringFormat={}{0:F0}%}"
                                               VerticalAlignment="Center" Foreground="#FBBF24"/>
                                    <Button Content="✕" Margin="10,0,0,0" Padding="6,0" FontSize="10"
                                            Style="{StaticResource GhostButton}"
                                            Command="{Binding DataContext.RemoveTargetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Visibility="{Binding IsCustom, Converter={StaticResource FlexVis}}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <TextBox Text="{Binding NewTargetHost, UpdateSourceTrigger=PropertyChanged}" Width="240"/>
                    <Button Content="Add target" Command="{Binding AddCustomTargetCommand}" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- ===== Sub-tabs: Ping / Traceroute / Speed ===== -->
        <TabControl Grid.Row="3" Margin="28,20,28,28" BorderThickness="0" Background="Transparent" Padding="0">
            <TabControl.Resources>
                <Style TargetType="TabItem" BasedOn="{StaticResource PillTabItem}"/>
            </TabControl.Resources>

            <!-- Ping tab -->
            <TabItem Header="Ping">
                <Border Style="{StaticResource Card}" Margin="0,14,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <DockPanel Grid.Row="0" Margin="0,0,0,12">
                            <TextBlock Text="Latency" Style="{StaticResource SectionTitle}"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right" VerticalAlignment="Center">
                                <TextBlock Text="Ping every" Style="{StaticResource Subtle}" VerticalAlignment="Center"/>
                                <TextBox Text="{Binding IntervalSeconds, UpdateSourceTrigger=PropertyChanged}" Width="48" Margin="8,0,6,0" TextAlignment="Center"/>
                                <TextBlock Text="s" Style="{StaticResource Subtle}" VerticalAlignment="Center"/>
                                <TextBlock Text="Window" Style="{StaticResource Subtle}" VerticalAlignment="Center" Margin="16,0,0,0"/>
                                <ComboBox SelectedValue="{Binding WindowSeconds}" ItemsSource="{Binding WindowOptions}" Width="82" Margin="8,0,0,0"/>
                            </StackPanel>
                        </DockPanel>
                        <lvc:CartesianChart Grid.Row="1"
                                            Background="Transparent"
                                            Series="{Binding LatencySeries}"
                                            XAxes="{Binding LatencyXAxes}"
                                            YAxes="{Binding LatencyYAxes}"
                                            LegendPosition="Bottom"
                                            LegendTextPaint="{Binding LegendTextPaint}"
                                            LegendBackgroundPaint="{Binding LegendBackgroundPaint}"
                                            TooltipTextPaint="{Binding TooltipTextPaint}"
                                            TooltipBackgroundPaint="{Binding TooltipBackgroundPaint}"/>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Traceroute tab -->
            <TabItem Header="Traceroute">
                <Grid Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Chart -->
                    <Border Grid.Column="0" Style="{StaticResource Card}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <DockPanel Grid.Row="0" Margin="0,0,0,12">
                                <TextBlock Text="Latency per hop" Style="{StaticResource SectionTitle}"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right" VerticalAlignment="Center">
                                    <TextBlock Text="Every" Style="{StaticResource Subtle}" VerticalAlignment="Center"/>
                                    <ComboBox ItemsSource="{Binding TraceIntervalOptions}"
                                              SelectedValue="{Binding TraceIntervalSeconds}" Width="72" Margin="8,0,4,0"/>
                                    <TextBlock Text="s" Style="{StaticResource Subtle}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DockPanel>
                            <lvc:CartesianChart Grid.Row="1"
                                                Background="Transparent"
                                                Series="{Binding TraceSeries}"
                                                XAxes="{Binding TraceXAxes}"
                                                YAxes="{Binding TraceYAxes}"
                                                LegendPosition="Bottom"
                                                LegendTextPaint="{Binding LegendTextPaint}"
                                                LegendBackgroundPaint="{Binding LegendBackgroundPaint}"
                                                TooltipTextPaint="{Binding TooltipTextPaint}"
                                                TooltipBackgroundPaint="{Binding TooltipBackgroundPaint}"/>
                        </Grid>
                    </Border>

                    <!-- Table + ad-hoc trace -->
                    <Border Grid.Column="1" Style="{StaticResource Card}" Margin="16,0,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Hops" Style="{StaticResource SectionTitle}"/>

                            <StackPanel Grid.Row="1" Margin="0,12,0,12">
                                <TextBlock Text="Run a one-off trace to any host:" Style="{StaticResource Subtle}" Margin="0,0,0,6"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBox Text="{Binding TraceHost, UpdateSourceTrigger=PropertyChanged}" Width="180"/>
                                    <Button Content="Trace now" Command="{Binding TraceCommand}" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0"
                                            IsEnabled="{Binding IsTracing, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>
                                    <Button Content="Cancel" Command="{Binding CancelTraceCommand}" Style="{StaticResource GhostButton}" Margin="6,0,0,0"/>
                                </StackPanel>
                                <TextBlock Text="{Binding TraceStatus}" Style="{StaticResource Subtle}" Margin="0,6,0,0"/>
                            </StackPanel>

                            <DataGrid Grid.Row="2" ItemsSource="{Binding TracerouteHops}"
                                      AutoGenerateColumns="False" HeadersVisibility="Column"
                                      Background="Transparent" BorderThickness="0"
                                      GridLinesVisibility="None" IsReadOnly="True"
                                      RowBackground="Transparent" AlternatingRowBackground="#0F141C">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="#" Binding="{Binding HopNumber}" Width="36"/>
                                    <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*"/>
                                    <DataGridTextColumn Header="ms" Binding="{Binding LatencyMs, StringFormat={}{0:F0}}" Width="60"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Speed test tab -->
            <TabItem Header="Speed test">
                <Grid Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- HTTP card -->
                    <Border Grid.Column="0" Style="{StaticResource Card}">
                        <StackPanel>
                            <DockPanel>
                                <StackPanel>
                                    <TextBlock Text="HTTP speed test" Style="{StaticResource SectionTitle}"/>
                                    <TextBlock Text="Measured against speed.cloudflare.com" Style="{StaticResource Subtle}" Margin="0,2,0,0"/>
                                </StackPanel>
                                <Button Content="Run" Command="{Binding RunHttpSpeedCommand}"
                                        Style="{StaticResource PrimaryButton}"
                                        HorizontalAlignment="Right" DockPanel.Dock="Right" Padding="18,8"
                                        IsEnabled="{Binding IsSpeedTesting, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>
                            </DockPanel>
                            <Grid Margin="0,16,0,0" Visibility="{Binding HttpResult, Converter={StaticResource FlexVis}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource CardInner}" Margin="0,0,6,0">
                                    <StackPanel>
                                        <TextBlock Text="DOWNLOAD" Style="{StaticResource Caption}"/>
                                        <TextBlock Text="{Binding HttpResult.DownloadMbps, StringFormat={}{0:F1} Mbps}" Style="{StaticResource Metric}" Foreground="#60A5FA"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="1" Style="{StaticResource CardInner}" Margin="6,0">
                                    <StackPanel>
                                        <TextBlock Text="UPLOAD" Style="{StaticResource Caption}"/>
                                        <TextBlock Text="{Binding HttpResult.UploadMbps, StringFormat={}{0:F1} Mbps}" Style="{StaticResource Metric}" Foreground="#4ADE80"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" Style="{StaticResource CardInner}" Margin="6,0,0,0">
                                    <StackPanel>
                                        <TextBlock Text="PING" Style="{StaticResource Caption}"/>
                                        <TextBlock Text="{Binding HttpResult.PingMs, StringFormat={}{0:F0} ms}" Style="{StaticResource Metric}" Foreground="#FBBF24"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                            <ProgressBar Value="{Binding SpeedProgress}" Maximum="100" Height="4" Margin="0,14,0,0"
                                         Visibility="{Binding IsHttpTesting, Converter={StaticResource FlexVis}}"/>
                        </StackPanel>
                    </Border>

                    <!-- Ookla card -->
                    <Border Grid.Column="1" Style="{StaticResource Card}" Margin="16,0,0,0">
                        <StackPanel>
                            <DockPanel>
                                <StackPanel>
                                    <TextBlock Text="Ookla speed test" Style="{StaticResource SectionTitle}"/>
                                    <TextBlock Text="Official CLI, downloaded on first run" Style="{StaticResource Subtle}" Margin="0,2,0,0"/>
                                </StackPanel>
                                <Button Content="Run" Command="{Binding RunOoklaSpeedCommand}"
                                        Style="{StaticResource PrimaryButton}"
                                        HorizontalAlignment="Right" DockPanel.Dock="Right" Padding="18,8"
                                        IsEnabled="{Binding IsSpeedTesting, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>
                            </DockPanel>
                            <Grid Margin="0,16,0,0" Visibility="{Binding OoklaResult, Converter={StaticResource FlexVis}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource CardInner}" Margin="0,0,6,0">
                                    <StackPanel>
                                        <TextBlock Text="DOWNLOAD" Style="{StaticResource Caption}"/>
                                        <TextBlock Text="{Binding OoklaResult.DownloadMbps, StringFormat={}{0:F1} Mbps}" Style="{StaticResource Metric}" Foreground="#60A5FA"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="1" Style="{StaticResource CardInner}" Margin="6,0">
                                    <StackPanel>
                                        <TextBlock Text="UPLOAD" Style="{StaticResource Caption}"/>
                                        <TextBlock Text="{Binding OoklaResult.UploadMbps, StringFormat={}{0:F1} Mbps}" Style="{StaticResource Metric}" Foreground="#4ADE80"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" Style="{StaticResource CardInner}" Margin="6,0,0,0">
                                    <StackPanel>
                                        <TextBlock Text="PING" Style="{StaticResource Caption}"/>
                                        <TextBlock Text="{Binding OoklaResult.PingMs, StringFormat={}{0:F0} ms}" Style="{StaticResource Metric}" Foreground="#FBBF24"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                            <TextBlock Text="{Binding OoklaResult.Server}" Style="{StaticResource Caption}" Margin="0,10,0,0"
                                       Visibility="{Binding OoklaResult, Converter={StaticResource FlexVis}}"/>
                            <ProgressBar Value="{Binding SpeedProgress}" Maximum="100" Height="4" Margin="0,14,0,0"
                                         Visibility="{Binding IsOoklaTesting, Converter={StaticResource FlexVis}}"/>
                            <TextBlock Text="{Binding SpeedStatus}" Style="{StaticResource Subtle}" Margin="0,6,0,0"/>
                            <Button Content="Cancel" Command="{Binding CancelSpeedCommand}" Style="{StaticResource GhostButton}" Margin="0,6,0,0"
                                    HorizontalAlignment="Left"
                                    Visibility="{Binding IsOoklaTesting, Converter={StaticResource FlexVis}}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Repair tab -->
            <TabItem Header="Repair">
                <Border Style="{StaticResource Card}" Margin="0,14,0,0">
                    <StackPanel>
                        <TextBlock Text="Network Repair Tools" Style="{StaticResource SectionTitle}"/>
                        <TextBlock Text="Common fixes for DNS, socket, and TCP/IP issues. Winsock and TCP/IP resets require admin and a reboot."
                                   Style="{StaticResource Subtle}" Margin="0,4,0,16"/>

                        <!-- DNS Flush -->
                        <Border Style="{StaticResource Card}" Padding="16" Margin="0,0,0,12">
                            <DockPanel>
                                <StackPanel>
                                    <TextBlock Text="Flush DNS Cache" FontWeight="SemiBold" FontSize="14"/>
                                    <TextBlock Text="Clears the local DNS resolver cache. Fixes stale DNS entries causing connection failures to specific sites. Safe, instant, no reboot."
                                               Style="{StaticResource Subtle}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap" MaxWidth="600"/>
                                </StackPanel>
                                <Button Content="Flush DNS" Command="{Binding FlushDnsCommand}"
                                        Style="{StaticResource PrimaryButton}"
                                        HorizontalAlignment="Right" DockPanel.Dock="Right" Padding="18,8"
                                        IsEnabled="{Binding IsRepairing, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>
                            </DockPanel>
                        </Border>

                        <!-- Winsock Reset -->
                        <Border Style="{StaticResource Card}" Padding="16" Margin="0,0,0,12">
                            <DockPanel>
                                <StackPanel>
                                    <TextBlock Text="Reset Winsock Catalog" FontWeight="SemiBold" FontSize="14"/>
                                    <TextBlock Text="Resets the Windows socket API to its default state. Fixes corrupted network drivers or broken socket connections. Requires admin + reboot."
                                               Style="{StaticResource Subtle}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap" MaxWidth="600"/>
                                </StackPanel>
                                <Button Content="Reset Winsock" Command="{Binding ResetWinsockCommand}"
                                        Style="{StaticResource SecondaryButton}"
                                        HorizontalAlignment="Right" DockPanel.Dock="Right" Padding="18,8"
                                        IsEnabled="{Binding IsRepairing, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>
                            </DockPanel>
                        </Border>

                        <!-- TCP/IP Reset -->
                        <Border Style="{StaticResource Card}" Padding="16" Margin="0,0,0,12">
                            <DockPanel>
                                <StackPanel>
                                    <TextBlock Text="Reset TCP/IP Stack" FontWeight="SemiBold" FontSize="14"/>
                                    <TextBlock Text="Restores all TCP/IP settings to factory defaults. Nuclear option for persistent connectivity issues. Requires admin + reboot."
                                               Style="{StaticResource Subtle}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap" MaxWidth="600"/>
                                </StackPanel>
                                <Button Content="Reset TCP/IP" Command="{Binding ResetTcpIpCommand}"
                                        Style="{StaticResource SecondaryButton}"
                                        HorizontalAlignment="Right" DockPanel.Dock="Right" Padding="18,8"
                                        IsEnabled="{Binding IsRepairing, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>
                            </DockPanel>
                        </Border>

                        <!-- Status -->
                        <TextBlock Text="{Binding RepairStatus}" Style="{StaticResource Subtle}" Margin="0,4,0,0"
                                   Visibility="{Binding RepairStatus, Converter={StaticResource FlexVis}}"/>
                        <TextBlock Text="⚠ A reboot is required for changes to take effect."
                                   Foreground="#EF4444" FontSize="12" Margin="0,8,0,0"
                                   Visibility="{Binding RepairNeedsReboot, Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
