<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SysManager · License: MIT -->
<UserControl x:Class="SysManager.Views.ProcessManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SysManager.ViewModels"
             d:DataContext="{d:DesignInstance vm:ProcessManagerViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource Surface0}">

    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Process Manager" Style="{StaticResource Display}"/>
            <TextBlock Text="View running processes, sort by memory or name, and kill unresponsive apps."
                       Style="{StaticResource Subtle}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Toolbar -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,12" Padding="12">
            <WrapPanel>
                <Button Content="⟳ Refresh" Command="{Binding RefreshCommand}"
                        Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>
                <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="8,0,8,0"
                           Style="{StaticResource Subtle}"/>
                <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                         Width="200" Margin="0,0,12,0"/>
                <TextBlock Text="Sort:" VerticalAlignment="Center" Margin="0,0,8,0"
                           Style="{StaticResource Subtle}"/>
                <Button Content="Memory" Command="{Binding SortByMemoryCommand}"
                        Style="{StaticResource GhostButton}" Margin="0,0,4,0"/>
                <Button Content="Name" Command="{Binding SortByNameCommand}"
                        Style="{StaticResource GhostButton}" Margin="0,0,4,0"/>
                <Button Content="PID" Command="{Binding SortByPidCommand}"
                        Style="{StaticResource GhostButton}"/>
            </WrapPanel>
        </Border>

        <!-- Summary -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,12" Padding="12">
            <TextBlock Text="{Binding Summary}" FontWeight="SemiBold" FontSize="13"/>
        </Border>

        <!-- Process list -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Padding="0">
            <ListView ItemsSource="{Binding FilteredProcesses}" Background="Transparent" BorderThickness="0"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      HorizontalContentAlignment="Stretch"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="12,6"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}"
                                            BorderBrush="{StaticResource Border1}" BorderThickness="0,0,0,1">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Surface2}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- PID -->
                            <TextBlock Grid.Column="0" Text="{Binding Pid}" FontSize="12"
                                       Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"
                                       FontFamily="Consolas"/>

                            <!-- Name + description -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="{Binding Description}" FontSize="11"
                                           Foreground="{StaticResource TextMuted}"
                                           TextTrimming="CharacterEllipsis" MaxWidth="400"/>
                            </StackPanel>

                            <!-- Memory -->
                            <TextBlock Grid.Column="2" Text="{Binding MemoryDisplay}" FontSize="12"
                                       VerticalAlignment="Center" HorizontalAlignment="Right"
                                       FontWeight="Medium"/>

                            <!-- Threads -->
                            <TextBlock Grid.Column="3" FontSize="11" VerticalAlignment="Center"
                                       HorizontalAlignment="Center" Foreground="{StaticResource TextMuted}">
                                <Run Text="{Binding ThreadCount, Mode=OneWay}"/>
                                <Run Text="thr"/>
                            </TextBlock>

                            <!-- Status -->
                            <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center"
                                        Margin="4,0">
                                <Ellipse Width="8" Height="8" VerticalAlignment="Center" Margin="0,0,6,0"
                                         Fill="{Binding Status, Converter={StaticResource StatusBrush}}"/>
                                <TextBlock Text="{Binding Status}" FontSize="11"
                                           Foreground="{Binding Status, Converter={StaticResource StatusBrush}}"
                                           VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Open location -->
                            <Button Grid.Column="5" Content="📂" ToolTip="Open file location"
                                    Command="{Binding DataContext.OpenFileLocationCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource GhostButton}" Padding="4,2" FontSize="12"
                                    Margin="4,0"/>

                            <!-- Kill -->
                            <Button Grid.Column="6" Content="✕" ToolTip="Kill process"
                                    Command="{Binding DataContext.KillProcessCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource GhostButton}" Padding="6,2" FontSize="12"
                                    Foreground="#EF4444"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <!-- Status bar -->
        <DockPanel Grid.Row="4" Margin="0,8,0,0">
            <ProgressBar DockPanel.Dock="Left" Width="120" Height="4" Margin="0,0,12,0"
                         IsIndeterminate="{Binding IsProgressIndeterminate}"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
            <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource Caption}"/>
        </DockPanel>
    </Grid>
</UserControl>
