<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SysManager · License: MIT -->
<UserControl x:Class="SysManager.Views.ShortcutCleanerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SysManager.ViewModels">
    <UserControl.DataContext>
        <vm:ShortcutCleanerViewModel />
    </UserControl.DataContext>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Broken Shortcut Cleaner" FontSize="20" FontWeight="SemiBold"
                       Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}" />
            <TextBlock Text="{Binding ScanStatus}" Opacity="0.7" Margin="0,4,0,0"
                       Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}" />
            <TextBlock Text="{Binding CurrentLocation}" Opacity="0.5" Margin="0,2,0,0"
                       Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"
                       Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibility}}" />
        </StackPanel>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="&#x1F50D;  Scan" Command="{Binding ScanCommand}" Padding="12,6" Margin="0,0,8,0" />
            <Button Content="&#x1F5D1;  Delete Selected" Command="{Binding DeleteSelectedCommand}" Padding="12,6" Margin="0,0,8,0" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" Padding="12,6" Margin="0,0,16,0"
                    Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibility}}" />
            <Separator Margin="0,0,16,0" />
            <Button Content="Select All" Command="{Binding SelectAllCommand}" Padding="8,6" Margin="0,0,4,0" />
            <Button Content="Deselect All" Command="{Binding DeselectAllCommand}" Padding="8,6" Margin="0,0,16,0" />
            <CheckBox Content="Move to Recycle Bin" IsChecked="{Binding MoveToRecycleBin}" VerticalAlignment="Center" />
        </StackPanel>

        <!-- Results list -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding BrokenShortcuts}"
                  AutoGenerateColumns="False" IsReadOnly="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  BorderThickness="1" BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="40" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="180" IsReadOnly="True" />
                <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="130" IsReadOnly="True" />
                <DataGridTextColumn Header="Target (missing)" Binding="{Binding TargetPath}" Width="*" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Opacity="0.6" VerticalAlignment="Center"
                       Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}">
                <Run Text="{Binding BrokenCount, Mode=OneWay}" />
                <Run Text=" broken · " />
                <Run Text="{Binding SelectedCount, Mode=OneWay}" />
                <Run Text=" selected" />
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
