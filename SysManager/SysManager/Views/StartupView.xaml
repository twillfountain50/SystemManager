<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SysManager · License: MIT -->
<UserControl x:Class="SysManager.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SysManager.ViewModels"
             d:DataContext="{d:DesignInstance vm:StartupViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource Surface0}">

    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Startup Manager" Style="{StaticResource Display}"/>
            <TextBlock Text="Programs that run when Windows starts. Disabling is non-destructive — you can re-enable anytime."
                       Style="{StaticResource Subtle}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Toolbar -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,12" Padding="12">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button Content="⟳ Refresh" Command="{Binding ScanCommand}"
                            Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>
                    <Button Content="Enable All" Command="{Binding EnableAllCommand}"
                            Style="{StaticResource GhostButton}" Margin="0,0,8,0"/>
                </StackPanel>
                <TextBlock Text="{Binding ScanSummary}" Style="{StaticResource Subtle}"
                           VerticalAlignment="Center" HorizontalAlignment="Right" DockPanel.Dock="Right"/>
            </DockPanel>
        </Border>

        <!-- Startup items list -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <ListView ItemsSource="{Binding Entries}" Background="Transparent" BorderThickness="0"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      HorizontalContentAlignment="Stretch">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}"
                                            BorderBrush="{StaticResource Border1}" BorderThickness="0,0,0,1">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Surface2}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Toggle -->
                            <CheckBox Grid.Column="0" IsChecked="{Binding IsEnabled}"
                                      Command="{Binding DataContext.ToggleEntryCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                      CommandParameter="{Binding}"
                                      VerticalAlignment="Center" Margin="0,0,12,0"/>

                            <!-- Name + command -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock FontSize="11" Foreground="{StaticResource TextMuted}"
                                           TextTrimming="CharacterEllipsis" MaxWidth="500">
                                    <Run Text="{Binding Publisher, Mode=OneWay}"/>
                                    <Run Text=" · " Foreground="{StaticResource TextDisabled}"/>
                                    <Run Text="{Binding Command, Mode=OneWay}"/>
                                </TextBlock>
                            </StackPanel>

                            <!-- Status badge -->
                            <Border Grid.Column="2" Style="{StaticResource Badge}" VerticalAlignment="Center" Margin="8,0">
                                <TextBlock Text="{Binding StatusText}" FontSize="11"/>
                            </Border>

                            <!-- Open location button -->
                            <Button Grid.Column="3" Content="📂" ToolTip="Open file location"
                                    Command="{Binding DataContext.OpenFileLocationCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource GhostButton}" Padding="8,4" FontSize="14"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <!-- Status bar -->
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Style="{StaticResource Caption}" Margin="0,8,0,0"/>
    </Grid>
</UserControl>
