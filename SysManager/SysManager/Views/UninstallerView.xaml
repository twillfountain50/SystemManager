<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SysManager · License: MIT -->
<UserControl x:Class="SysManager.Views.UninstallerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SysManager.ViewModels"
             d:DataContext="{d:DesignInstance vm:UninstallerViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource Surface0}">

    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Uninstaller" Style="{StaticResource Display}"/>
            <TextBlock Text="List installed applications and remove the ones you no longer need."
                       Style="{StaticResource Subtle}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Toolbar -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,12" Padding="12">
            <WrapPanel>
                <Button Content="⟳ Scan" Command="{Binding ScanCommand}"
                        Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>
                <Button Content="🗑 Uninstall selected" Command="{Binding UninstallSelectedCommand}"
                        Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>
                <Button Content="✕ Cancel" Command="{Binding CancelCommand}"
                        Style="{StaticResource GhostButton}" Margin="0,0,12,0"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="8,0,8,0"
                           Style="{StaticResource Subtle}"/>
                <Grid Width="220" Margin="0,0,12,0">
                    <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Search apps…" IsHitTestVisible="False"
                               Foreground="{StaticResource TextMuted}" Margin="6,0,0,0"
                               VerticalAlignment="Center" FontSize="12"
                               Visibility="{Binding FilterText, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>
                </Grid>
                <Button Content="Select all" Command="{Binding SelectAllCommand}"
                        Style="{StaticResource GhostButton}" Margin="0,0,4,0"/>
                <Button Content="Deselect" Command="{Binding DeselectAllCommand}"
                        Style="{StaticResource GhostButton}" Margin="0,0,12,0"/>
                <TextBlock Text="Sort:" VerticalAlignment="Center" Margin="0,0,8,0"
                           Style="{StaticResource Subtle}"/>
                <Button Content="Name" Command="{Binding SortByNameCommand}"
                        Style="{StaticResource GhostButton}" Margin="0,0,4,0"/>
                <Button Content="Size" Command="{Binding SortBySizeCommand}"
                        Style="{StaticResource GhostButton}" Margin="0,0,4,0"/>
                <Button Content="Publisher" Command="{Binding SortByPublisherCommand}"
                        Style="{StaticResource GhostButton}"/>
            </WrapPanel>
        </Border>

        <!-- Summary -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,12" Padding="12">
            <TextBlock Text="{Binding Summary}" FontWeight="SemiBold" FontSize="13"/>
        </Border>

        <!-- App list -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Padding="0">
            <ListView ItemsSource="{Binding FilteredApps}" Background="Transparent" BorderThickness="0"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      HorizontalContentAlignment="Stretch"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="12,6"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border x:Name="Bd" Background="Transparent"
                                            Padding="{TemplateBinding Padding}"
                                            BorderBrush="{StaticResource Border1}" BorderThickness="0,0,0,1">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background"
                                                    Value="{StaticResource Surface2}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="32"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="220"/>
                            </Grid.ColumnDefinitions>

                            <!-- Checkbox -->
                            <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}"
                                      VerticalAlignment="Center"/>

                            <!-- Name + Id -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="{Binding Id}" FontSize="11"
                                           Foreground="{StaticResource TextMuted}"
                                           TextTrimming="CharacterEllipsis" MaxWidth="400"/>
                            </StackPanel>

                            <!-- Size -->
                            <TextBlock Grid.Column="2" Text="{Binding SizeDisplay}" FontSize="12"
                                       VerticalAlignment="Center" HorizontalAlignment="Right"
                                       Foreground="{StaticResource TextSecondary}" FontFamily="Consolas"/>

                            <!-- Version -->
                            <TextBlock Grid.Column="3" Text="{Binding Version}" FontSize="12"
                                       VerticalAlignment="Center" HorizontalAlignment="Right"
                                       Foreground="{StaticResource TextMuted}" FontFamily="Consolas"/>

                            <!-- Source (show 'Local' for ARP-only apps without winget source) -->
                            <TextBlock Grid.Column="4" FontSize="11"
                                       VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding Source}"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Source}" Value="">
                                                <Setter Property="Text" Value="Local"/>
                                                <Setter Property="Foreground" Value="#F59E0B"/>
                                                <Setter Property="ToolTip" Value="Not from winget — uninstall may require the app's own installer"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <!-- Status -->
                            <Border Grid.Column="5" Style="{StaticResource Badge}"
                                    VerticalAlignment="Center" Margin="4,0"
                                    Visibility="{Binding Status, Converter={StaticResource FlexVis}}"
                                    ToolTip="{Binding Status}">
                                <TextBlock Text="{Binding Status}" FontSize="11"
                                           TextTrimming="CharacterEllipsis" MaxWidth="200"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <!-- Status bar -->
        <DockPanel Grid.Row="4" Margin="0,8,0,0">
            <ProgressBar DockPanel.Dock="Left" Width="120" Height="4" Margin="0,0,12,0"
                         IsIndeterminate="{Binding IsProgressIndeterminate}"
                         Value="{Binding Progress}"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
            <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource Caption}"/>
        </DockPanel>
    </Grid>
</UserControl>
