<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SysManager · License: MIT -->
<UserControl x:Class="SysManager.Views.WindowsUpdateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:v="clr-namespace:SysManager.Views">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="PageBg" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#070A0F" Offset="0"/>
            <GradientStop Color="#0B1220" Offset="0.5"/>
            <GradientStop Color="#090D16" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Resources>

    <Grid Background="{StaticResource PageBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="28,24,28,0">
            <TextBlock Text="Windows Update" Style="{StaticResource Display}"/>
            <TextBlock Text="Check, list and install Windows updates — powered by PSWindowsUpdate."
                       Style="{StaticResource Subtle}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Admin banner — visible only when NOT elevated -->
        <Border Grid.Row="1" Margin="28,16,28,0" Padding="16,14" CornerRadius="10"
                Background="#2A1E14" BorderBrush="#F59E0B" BorderThickness="1"
                Visibility="{Binding IsElevated, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE7EF;" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets"
                                   FontSize="18" Foreground="#F59E0B" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="These actions need administrator rights"
                                   FontSize="15" FontWeight="SemiBold" Foreground="#F59E0B" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="Windows Update COM APIs can't list or install updates without elevation. Click the button to restart the app as admin — you'll see the same tabs but everything will work."
                               Foreground="{StaticResource TextSecondary}" Margin="28,6,0,0" TextWrapping="Wrap"/>
                </StackPanel>
                <Button Grid.Column="1" Content="Run as Administrator"
                        Command="{Binding RelaunchAsAdminCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Padding="18,10" FontSize="14" VerticalAlignment="Center" Margin="16,0,0,0"/>
            </Grid>
        </Border>

        <!-- Module status -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="28,16,28,0"
                BorderBrush="#F59E0B" BorderThickness="1"
                Visibility="{Binding ModuleAvailable, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE946;" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets"
                                   FontSize="18" Foreground="#F59E0B" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="PSWindowsUpdate module is required" FontSize="15" FontWeight="SemiBold"
                                   Foreground="#F59E0B" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding ModuleStatus}" Style="{StaticResource Subtle}" Margin="28,6,0,0" TextWrapping="Wrap"/>
                    <TextBlock Text="Click Install to add it from the PowerShell Gallery. Needs administrator rights."
                               Style="{StaticResource Subtle}" Margin="28,2,0,0" TextWrapping="Wrap"/>
                </StackPanel>
                <Button Grid.Column="1" Content="Install PSWindowsUpdate"
                        Command="{Binding InstallModuleCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Padding="18,10" FontSize="14" VerticalAlignment="Center" Margin="16,0,0,0"/>
            </Grid>
        </Border>

        <!-- Module available — quiet green pill -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="28,16,28,0"
                Visibility="{Binding ModuleAvailable, Converter={StaticResource FlexVis}}">
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,10,0" Fill="#22C55E"/>
                <TextBlock Text="PSWindowsUpdate is available" FontWeight="SemiBold" Foreground="#22C55E" VerticalAlignment="Center"/>
                <TextBlock Text="  ·  " Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding ModuleStatus}" Style="{StaticResource Subtle}" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Action buttons -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Margin="28,16,28,0">
            <StackPanel>
                <TextBlock Text="Actions" Style="{StaticResource SectionTitle}" Margin="0,0,0,10"/>
                <WrapPanel Orientation="Horizontal">
                    <Button Content="Check module" Command="{Binding CheckModuleCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8"/>
                    <Button Content="Install PSWindowsUpdate" Command="{Binding InstallModuleCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8"/>
                    <Button Content="List updates" Command="{Binding ListUpdatesCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,8"/>
                    <Button Content="Feature upgrades" Command="{Binding ListFeatureUpdatesCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8"/>
                    <Button Content="History" Command="{Binding ShowHistoryCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8"/>
                    <Button Content="Pending reboot?" Command="{Binding CheckPendingRebootCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8"/>
                    <Button Content="Install updates" Command="{Binding InstallUpdatesCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,8"/>
                    <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource GhostButton}" Margin="0,0,8,8"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- Live output -->
        <Border Grid.Row="4" Style="{StaticResource Card}" Margin="28,16,28,0" Padding="0">
            <v:ConsoleView DataContext="{Binding Console}"/>
        </Border>

        <!-- Footer -->
        <Grid Grid.Row="5" Margin="28,12,28,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ProgressBar Grid.Column="0" Height="6" Minimum="0" Maximum="100"
                         Value="{Binding Progress}"
                         IsIndeterminate="{Binding IsProgressIndeterminate}"/>
            <TextBlock Grid.Column="1" Margin="12,0,0,0" Text="{Binding StatusMessage}" Style="{StaticResource Subtle}"/>
        </Grid>
    </Grid>
</UserControl>
